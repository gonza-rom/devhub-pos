---
import { Icon } from 'astro-icon/components';

const WHATSAPP_ENTERPRISE = 'https://wa.me/543834946767?text=Hola%2C%20me%20interesa%20el%20plan%20Enterprise%20de%20DevHub%20POS';

const features = [
  'Productos ilimitados',
  'Hasta 3 usuarios',
  'Reportes avanzados',
  'Alertas de stock',
  'Soporte prioritario',
  'Backups diarios',
];
---

<section id="pricing" class="relative z-10 py-28 px-[5vw]">
  <div class="max-w-[1200px] mx-auto">

    <!-- Header -->
    <div class="pricing-header text-center mb-16">
      <span class="block text-[0.7rem] font-semibold tracking-[0.12em] uppercase text-red mb-4">
        Planes y precios
      </span>
      <h2 class="font-head font-extrabold tracking-tight leading-[1.1] text-[clamp(2rem,3.5vw,3rem)]">
        Empezá gratis.<br />Escalá cuando crezcas.
      </h2>
      <p class="text-gray-500 font-light mt-4 text-sm">
        7 días de prueba gratis en todos los planes. Sin tarjeta de crédito.
      </p>
    </div>

    <!-- Two cards: Trial + Pro -->
    <div class="grid md:grid-cols-2 gap-6 max-w-[780px] mx-auto mb-8">

      <!-- Trial card -->
      <div class="plan-card relative rounded-xl p-8 border border-white/[0.07] bg-card
                  hover:border-white/20 transition-all duration-300 flex flex-col"
           style="--delay: 0ms">
        <p class="font-head font-bold text-xs uppercase tracking-widest text-gray-500 mb-1">Prueba gratuita</p>
        <p class="text-gray-600 text-xs mb-5">Conocé el sistema sin compromiso.</p>

        <p class="font-head font-extrabold text-6xl tracking-tight leading-none mb-1">7 días</p>
        <p class="text-sm text-gray-500 mb-6">acceso completo · sin tarjeta</p>

        <div class="h-px bg-white/[0.07] mb-6" />

        <ul class="flex flex-col gap-3 mb-8 list-none p-0 m-0 flex-1">
          {[
            'Todas las funciones Pro',
            'Sin tarjeta de crédito',
            'Configuración en minutos',
            'Soporte incluido',
          ].map((f, j) => (
            <li class="plan-feature flex items-center gap-2.5 text-sm text-gray-400"
                style={`--fdelay: ${j * 60}ms`}>
              <Icon name="lucide:check" class="w-4 h-4 shrink-0 text-gray-500" />
              {f}
            </li>
          ))}
        </ul>

        <a href="https://app.devhubpos.com/register"
           target="_blank" rel="noopener noreferrer"
           class="flex items-center justify-center gap-2 w-full py-3 rounded-md text-sm font-medium
                  border border-white/[0.07] text-white hover:border-white/25 hover:bg-white/[0.04]
                  transition-all duration-200 no-underline">
          <Icon name="lucide:play-circle" class="w-4 h-4" /> Empezar prueba
        </a>
        <p class="text-center text-xs text-gray-600 mt-3">Al terminar elegís si continuás o no</p>
      </div>

      <!-- Pro card -->
      <div class="plan-card relative rounded-xl p-8 border border-red flex flex-col"
           style="--delay: 120ms; background: linear-gradient(160deg, rgba(220,38,38,0.08) 0%, #161616 50%)">

        <div class="absolute -top-3 left-1/2 -translate-x-1/2
                    bg-red text-white text-[0.65rem] font-bold
                    uppercase tracking-widest px-3.5 py-1 rounded-full whitespace-nowrap"
             style="animation: badge-pulse 2s ease-in-out infinite">
          Plan completo
        </div>

        <p class="font-head font-bold text-xs uppercase tracking-widest text-gray-500 mb-1">Pro</p>
        <p class="text-gray-500 text-xs mb-5">Para comercios que quieren control total.</p>

        <p class="font-head font-extrabold text-6xl tracking-tight leading-none mb-1">
          <sup class="text-3xl align-super">$</sup>35.000
        </p>
        <p class="text-sm text-gray-500 mb-6">por mes · hasta 3 usuarios</p>

        <div class="h-px bg-white/[0.07] mb-6" />

        <ul class="flex flex-col gap-3 mb-8 list-none p-0 m-0 flex-1">
          {features.map((f, j) => (
            <li class="plan-feature flex items-center gap-2.5 text-sm text-gray-300"
                style={`--fdelay: ${j * 60}ms`}>
              <Icon name="lucide:check" class="w-4 h-4 shrink-0 text-red" />
              {f}
            </li>
          ))}
        </ul>

        <a href="https://app.devhubpos.com/register?plan=pro"
           target="_blank" rel="noopener noreferrer"
           class="flex items-center justify-center gap-2 w-full py-3.5 rounded-md text-sm font-medium
                  bg-red hover:bg-red-dk text-white transition-all duration-200 no-underline
                  hover:shadow-glow hover:-translate-y-px"
           style="animation: btn-glow 2.5s ease-in-out infinite">
          <Icon name="lucide:user-plus" class="w-4 h-4" /> Suscribirme al Pro
        </a>
        <p class="text-center text-xs text-gray-600 mt-3">Cancelás cuando querés</p>
      </div>

    </div>

    <!-- Price note -->
    <p class="text-center text-xs text-gray-600 mb-16">
      Precios en pesos argentinos · Se actualizan trimestralmente según inflación
    </p>

    <!-- Enterprise block -->
    <div class="enterprise-card relative rounded-xl border border-white/[0.07] bg-card overflow-hidden">
      <!-- Subtle gradient accent -->
      <div class="absolute inset-0 pointer-events-none"
           style="background: linear-gradient(90deg, rgba(220,38,38,0.04) 0%, transparent 50%)" />

      <div class="relative z-10 flex flex-col md:flex-row items-start md:items-center
                  justify-between gap-8 p-8 sm:p-10">
        <div class="flex-1">
          <div class="flex items-center gap-2 mb-3">
            <div class="w-7 h-7 rounded-md flex items-center justify-center border border-red/25"
                 style="background: rgba(220,38,38,0.1)">
              <Icon name="lucide:building-2" class="w-3.5 h-3.5 text-red" />
            </div>
            <span class="text-[0.7rem] font-bold tracking-[0.1em] uppercase text-red">Enterprise</span>
          </div>
          <h3 class="font-head font-extrabold text-xl sm:text-2xl mb-2">
            ¿Tenés un negocio más grande?
          </h3>
          <p class="text-gray-500 text-sm leading-relaxed max-w-[50ch]">
            Para comercios medianos con múltiples sucursales, usuarios ilimitados y
            base de datos dedicada. Sistema a medida, cotización personalizada.
          </p>

          <!-- What's included -->
          <div class="flex flex-wrap gap-x-6 gap-y-2 mt-5">
            {[
              'Base de datos dedicada',
              'Usuarios ilimitados',
              'Multi-sucursal',
              'API e integraciones',
              'Onboarding dedicado',
              'Soporte 24/7',
            ].map((f) => (
              <span class="flex items-center gap-1.5 text-xs text-gray-400">
                <Icon name="lucide:check" class="w-3 h-3 text-red shrink-0" /> {f}
              </span>
            ))}
          </div>
        </div>

        <!-- CTA -->
        <div class="flex flex-col gap-3 shrink-0 w-full md:w-auto">
          <a href={WHATSAPP_ENTERPRISE}
             target="_blank" rel="noopener noreferrer"
             class="inline-flex items-center justify-center gap-2 px-7 py-3.5 rounded-md
                    font-medium text-sm text-white transition-all duration-200 no-underline
                    hover:-translate-y-px"
             style="background: #25D366;"
             onmouseover="this.style.boxShadow='0 6px 20px rgba(37,211,102,0.3)'"
             onmouseout="this.style.boxShadow='none'">
            <svg class="w-4 h-4 shrink-0" viewBox="0 0 24 24" fill="currentColor">
              <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/>
            </svg>
            Hablar por WhatsApp
          </a>
          <a href="mailto:devhubpos@gmail.com"
             class="inline-flex items-center justify-center gap-2 px-7 py-3 rounded-md
                    font-medium text-sm border border-white/[0.07] text-gray-400
                    hover:border-white/20 hover:text-white transition-all duration-200 no-underline">
            <Icon name="lucide:mail" class="w-4 h-4" /> Enviar email
          </a>
          <p class="text-center text-[0.65rem] text-gray-600">Respondemos en menos de 24hs</p>
        </div>
      </div>
    </div>

  </div>
</section>

<style>
  .pricing-header {
    opacity: 0; transform: translateY(24px);
    transition: opacity 0.6s ease, transform 0.6s ease;
  }
  .pricing-header.visible { opacity: 1; transform: translateY(0); }

  .plan-card {
    opacity: 0; transform: translateY(40px);
    transition: opacity 0.5s ease, transform 0.5s ease, border-color 0.3s, box-shadow 0.3s;
    transition-delay: var(--delay);
  }
  .plan-card.visible { opacity: 1; transform: translateY(0); }
  .plan-card.visible:hover { transform: translateY(-6px); }

  .plan-feature {
    opacity: 0; transform: translateX(-8px);
    transition: opacity 0.3s ease, transform 0.3s ease;
    transition-delay: var(--fdelay);
  }
  .plan-card.visible .plan-feature { opacity: 1; transform: translateX(0); }

  .enterprise-card {
    opacity: 0; transform: translateY(24px);
    transition: opacity 0.6s 0.3s ease, transform 0.6s 0.3s ease;
  }
  .enterprise-card.visible { opacity: 1; transform: translateY(0); }

  @keyframes badge-pulse {
    0%, 100% { box-shadow: 0 0 0 0 rgba(220,38,38,0.4); }
    50%       { box-shadow: 0 0 0 6px rgba(220,38,38,0); }
  }
  @keyframes btn-glow {
    0%, 100% { box-shadow: 0 0 0 0 rgba(220,38,38,0); }
    50%       { box-shadow: 0 0 20px 4px rgba(220,38,38,0.3); }
  }
</style>

<script>
  const io = new IntersectionObserver(
    (entries) => entries.forEach((e) => e.isIntersecting && e.target.classList.add('visible')),
    { threshold: 0.1 }
  );
  document.querySelectorAll('.pricing-header, .plan-card, .enterprise-card').forEach((el) => io.observe(el));
</script>